{
  "$schema": "http://json-schema.org/draft-07/schema",
  "type": "object",
  "properties": {
    "status": { "type": "string", "description": "Result status (success)." },
    "message": { "type": "string", "description": "Human-readable result message." },
    "objectStore": { "type": "string", "description": "Name of the object store that was reset." },
    "entriesRemoved": { "type": "integer", "description": "Number of entries removed from the object store." }
  }
}
